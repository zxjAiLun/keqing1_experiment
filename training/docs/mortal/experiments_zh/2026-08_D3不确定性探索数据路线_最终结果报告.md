# 2026-08 D3 不确定性探索数据路线 最终结果报告

> 本报告是 `D3_uncertainty_guided_exploration`（`D3_top2_discard_v1`）从设计到最终 adjudication 的完整受控实验闭环记录。机器可读状态以 `research_registry.json` 的 D3 record（`gate_record` / `aggregate_record` / `promotion_summary`）为准。

## 全生命周期

```text
设计（预注册设计 2026-08_D3不确定性探索_预注册设计.md）
→ 25h authoritative smoke（703 eligible / 129 explored，provenance 全一致）
→ B250 production gate（seeds 1800000..1800249，audit-v2 74/74 PASS；audit-v1 无效化）
→ 24-shard 6000h generation（1800000..1805999，24/24 PASS）
→ aggregate closure（27/27 PASS，151282 eligible / 27506 explored）
→ training-view/target contract（6000 K0 perspectives / 968265 rows / final_rank_mc targets，manifest frozen）
→ training-recipe governance（3 configs 继承 M0 recipe，preserved Adam，70k→72k）
→ 3 × 70k→72k 训练（20260806/07/08，3/3 completion PASS）
→ 3 × 1000h matched evaluation（1700000/1710000/1720000）
→ summary v1 invalidated（ReachAccepted −1000 缺陷）
→ Repair 1（8b36ee8，native Stat equivalence 12000/12000 零差异）
→ v2 authoritative adjudication（8b36ee8）
→ NOT PROMOTED
```

## 工程执行 vs 科学结论

```text
generation integrity      ✅ PASS / CLOSED
training execution        ✅ PASS / COMPLETE
experimental checkpoints  ✅ 存在并冻结（3 个 D3 72k：a93e7a8f…/78d78a59…/5790cbb0…）
research promotion        ❌ D3 data route NOT PROMOTED
                          ❌ 无 checkpoint 晋级
                          ❌ K1 未产生（null）
```

工程执行成功 ≠ 科学假设晋级成功：D3 是前者非常完整、后者明确失败的 controlled experiment。

## 最终统计（v2 authoritative，bootstrap reps 5000，seed 20260802/03/04）

```text
D3 − M0（primary）
  seed means       -0.855 / -11.025 / -2.610   （0/3 正向）
  pooled           -4.830 Pt/hanchan
  pooled CI95      [-9.840, +0.015]
  hierarchical     [-12.060, +2.070]
  sign test        0/3，p = 1.0

D3 − K0（secondary）
  seed means       +2.610 / -6.840 / -5.310
  pooled           -3.180
  hierarchical     [-9.690, +3.750]

M0 − K0（descriptive control）
  pooled           +1.650
  hierarchical     [-4.530, +7.605]
```

预注册 promotion gate（合取条件，不允许事后改口径）：

```text
route_pass = all(seed_mean > 0 for D3−M0) AND hierarchical CI95 lower > 0
          = false（0/3 方向；CI 无法救回已失败的合取条件）
k1_pass    = route_pass AND D3−K0 hier CI lower > 0 AND integrity = false
```

## 统计口径与 provenance

- paired unit = complete hanchan；final ranks 从 raw logs 独立重建，并逐 hanchan 与 native `libriichi.stat.Stat.from_log` 对账（3000 × 4 = 12000 player-ranks，零差异）。
- summary v1（`a6b1d4b`）因 final-score reconstruction 遗漏 `ReachAccepted −1000` 被判定 invalidated 并保留为历史；v2（`8b36ee8`）为权威口径。
- raw evaluation / training 从未重跑、从未修改。

```text
raw_evaluator_commit    413a268…
summary_v1_commit       a6b1d4b…（invalidated）
summary_v2_commit       8b36ee8…
summary_v2 json         e8d0d9e2…   md 03fc810d…
promotion_decision      c205e1ef…
reconstruction_equiv    af94270b…
```

## 结论

`D3_top2_discard_v1` 在完整 generation → training → matched promotion evaluation 链路中未优于 M0，按预注册规则机械拒绝晋级。**本结果拒绝当前 D3 数据路线，不证明 uncertainty-guided exploration 这一更宽泛思想整体无效**；若继续研究必须建立新的 experiment ID，不得修改 D3 后继续沿用同一实验身份。

D1/D2 历史 summary 因同一 ReachAccepted 缺陷用修正口径重算（方向不变，effect sizes/CI 已更正，见 registry `historical_summary_correction`）。D1/D2/D3 这一整条 controlled training diagnostics 分支至此形成历史闭环。
