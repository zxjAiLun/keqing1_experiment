# D3 generation v1 smoke 结果

## 结论

`D3_top2_discard_v1` 的 authoritative clean 25h function smoke 已通过。A/B 两次都从空目录开始，使用完全相同的 25 个 smoke seed；探索事件、确定性 hash、去除非决定性 metadata 后的牌谱 hash 全部一致。

此前在实现提交前运行的 `run_a/run_b` 仅保留为 development smoke，已被本次 authoritative smoke supersede，不作为正式 production 授权依据。

本报告只确认生成器、native context 传递和审计链路正确，不使用 Pt、平均顺位或行为指标判断 D3 是否有强度收益。production 250h shard 和完整 6000h 仍未启动。

## Authoritative lineage

| 项目 | 值 |
| --- | --- |
| project commit | `7eb48f1310e917cfb2f1f45445e546b4e92d1a89` |
| native Mortal commit | `813859fc8110ea178f56f009994bc4f1b9fee645` |
| D3 native patch SHA256 | `fa7ff1e12687c3ba7ec4d5ea47902ded2c263f7695e0da195e0daabf437ffed1` |
| loaded `riichi.pyd` SHA256 | `19bb181eaa70d0ae90417a3bd22433f6ca08d7654602f865ff3bdb102b7d9914` |
| project/native dirty | `false / false` |
| native build | release |
| GPU / CUDA | NVIDIA GeForce RTX 4060 Laptop GPU；AMP=false |

## 固定协议

| 项目 | 值 |
| --- | --- |
| contract | `D3_top2_discard_v1` |
| models | `K0_70k / V2_74000 / V3_74000 / ext_mortal` |
| K0 exploration | 只有 `K0_70k` 启用；其余三家保持冻结策略 |
| seed | `1799000..1799024`，`seed_key=8192` |
| batch | 25 hanchans，native batch 25 |
| seat | random |
| device | CUDA；AMP=false |
| trigger | top-1/top-2 均为 discard，自己未立直，margin `<=0.5` |
| sampling | canonical SHA-256 hash，`u < 0.25` |
| budget | 每 kyoku 最多 1 次，每 hanchan 最多 8 次 |

Rust native context 明确区分 primary action 与 auxiliary kan guard row。只有 primary action 允许探索；auxiliary row 不改变动作、不消费预算、不写 exploration event。

## 结果

两次 authoritative 运行都得到：

- 25 个日志文件、25 个唯一 seed、0 个 malformed log；
- 4063 个 primary decision states；
- 703 个 eligible decision；
- 129 个实际 top-2 exploration；
- 351 个 `hash_rejected`；
- 207 个 `kyoku_budget_exhausted`；
- 16 个 `hanchan_budget_exhausted`；
- `auxiliary_exploration_count=0`；
- 0 个 own-riichi exploration；
- 0 个 discard→reach/pass/chi/pon/kan 等跨语义 exploration；
- 每个 kyoku 和每个 hanchan 的 exploration budget 均未超限。

A/B 的 703 条排序后事件记录完全相同；25 个 seed 的去除 `meta` 牌谱 canonical SHA-256 完全相同。

## 完整 pytest 基线

完整 pytest 不是 D3 gate，但已保存 before/after 机器基线：

- parent `b6b188b`：293 passed，21 failed；
- D3 hardening `7eb48f1`：293 passed，21 failed；
- failure nodeid 集合相同；新增失败 0，恢复失败 0；
- 失败主要是现有 ladder fixture/report 目录缺失，不是 D3 路径。

机器文件：

- `artifacts/experiments/model_pool_2026_07/D3_uncertainty_guided_exploration_2026_08/validation/pytest_full_report_before.txt`
- `artifacts/experiments/model_pool_2026_07/D3_uncertainty_guided_exploration_2026_08/validation/pytest_full_report_after.txt`
- `artifacts/experiments/model_pool_2026_07/D3_uncertainty_guided_exploration_2026_08/validation/pytest_failure_baseline.json`

## 产物

机器审计：

`artifacts/experiments/model_pool_2026_07/D3_uncertainty_guided_exploration_2026_08/generation_smoke/d3_smoke_audit.json`

机器审计 Markdown：

`artifacts/experiments/model_pool_2026_07/D3_uncertainty_guided_exploration_2026_08/generation_smoke/d3_smoke_audit.md`

authoritative run 目录：

- `generation_smoke/authoritative_run_a`
- `generation_smoke/authoritative_run_b`

实现入口：

- `scripts/mortal/d3_exploration_engine.py`
- `scripts/mortal/run_d3_exploration_smoke_2026_08.py`
- `scripts/mortal/audit_d3_exploration_smoke_2026_08.py`

## 下一道 gate

D3 状态仍保持 `preregistered_not_started`，因为 smoke 不是 production gate。下一步只能运行 `1800000..1800249` 的首个 250h B250 production shard；该 shard 通过后才允许继续剩余 5750 个 hanchan。不能根据本次 smoke 的 Pt 或行为结果修改概率、阈值或预算。
