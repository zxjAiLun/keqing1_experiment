# C3 D1_CQL_OFF 绝对强度晋级实验：结果报告（closure）

> 实验状态：**已完成并收口（CLOSED）；判定为 `not_supported`；明确拒绝绝对强度晋级；不晋级任何 recipe 或 checkpoint；K1 仍不存在。**

## 1. 实验身份与研究目的

- **实验 ID**: `C3_d1_cql_off_absolute_promotion_2026_08`
- **预注册设计**: [`2026-08_C3_D1_CQL_OFF绝对强度晋级实验_预注册设计.md`](2026-08_C3_D1_CQL_OFF绝对强度晋级实验_预注册设计.md)
- **实验目的**: 使用全新、完全独立的 3,000 局四人对局评测数据（12 shards × 250 games，对局 ID 区间 1900000..1920999），在严格同桌控制下检验现有 `D1_CQL_OFF` checkpoints 是否在绝对强度上同时优于：
  1. 正式 lineage 根模型 **`K0_70k`**
  2. 正式 operational continuation control **`M0_CURRENT`**

## 2. 关键 Provenance 与 Artifacts

```text
implementation commit  0726fd8b2db4d528e1f18b70805a6e2194be831b
evaluator path         training/mortal/four_player_native.py
c3_summary.json SHA256 44beb5ca34e4545b72adb2eb259f5e7da9ca7da9ca2da62250c8599eaf76cdb0
total unique logs      3000 (12 shards x 250 logs/shard)
```

## 3. 门禁核验结果

全部 3 项硬门禁一次性 **PASS**：
- `checkpoints_verified`: **PASS**（8 个固定模型 checkpoint 文件与 SHA-256 全量精确匹配）
- `shards_complete`: **PASS**（全部 12 个分片、每片 250 局对局日志解析完整）
- `total_3000_unique_games_verified`: **PASS**（3,000 个独立原始 JSONL 对局日志，无重复 ID）

## 4. 统计结果与判定

### 4.1 $x$ 统计：$\text{Pt}(\text{D1\_CQL\_OFF}) - \text{Pt}(\text{K0\_70k})$
- **Seed means (`20260806 / 20260807 / 20260808`)**:
  - `20260806`: `-11.385` Pt/半庄
  - `20260807`: `-21.555` Pt/半庄
  - `20260808`: `-2.205` Pt/半庄
  - **3/3 seed means 为负**（全正判定: **FAIL**）
- **Equal-seed mean**: `-11.715` Pt/半庄
- **Hierarchical CI95**: `[-21.945, -1.845]`（CI95 下限 > 0: **FAIL**，区间完全低于 0）

### 4.2 $y$ 统计：$\text{Pt}(\text{D1\_CQL\_OFF}) - \text{Pt}(\text{M0\_CURRENT})$
- **Seed means (`20260806 / 20260807 / 20260808`)**:
  - `20260806`: `-16.605` Pt/半庄
  - `20260807`: `-18.270` Pt/半庄
  - `20260808`: `-7.155` Pt/半庄
  - **3/3 seed means 为负**（全正判定: **FAIL**）
- **Equal-seed mean**: `-14.010` Pt/半庄
- **Hierarchical CI95**: `[-20.820, -6.419]`（CI95 下限 > 0: **FAIL**，区间完全低于 0）

## 5. 最终机器结论与决策边界

```text
verdict              not_supported
recipe_promotion     false
checkpoint_promotion false
K1                   null
```

### 科学解释与禁令
1. **明确负向结论**: `D1_CQL_OFF` 无论对比 `K0_70k` 还是 `M0_CURRENT`，在 3 个 training seeds 上全部为负，且 95% 置信区间上限均显著低于 0。这不是“证据不足”，而是对绝对强度优势的明确拒绝。
2. **与 C1 的关系**: C1 的 DID interaction finding（+11.550 Pt/半庄）代表“CQL removal 相对减轻了 D1 的损伤幅度”，但 D1 语料本身的绝对质量不足，关闭 CQL 后的绝对强度依然大幅落后于 baseline。
3. **分支终止与禁令**:
   - `D1_CQL_OFF` 分支正式终止收口；
   - 严禁继续对 D1 或 CQL_OFF 进行调参、追加评测或扫描；
   - 不再沿用 C1/C2/C3 分支，K1 仍为 `null`，下一阶段将进行全新的路线重置（K1 route reset）。
